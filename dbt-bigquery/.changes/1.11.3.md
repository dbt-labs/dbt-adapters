## dbt-bigquery 1.11.3 - June 24, 2026

### Fixes

- When persist_docs.columns is enabled, dbt now warns about columns defined in YAML that don't exist in the database via the validate_doc_columns helper, with STRUCT-aware column name matching. ([#1690](https://github.com/dbt-labs/dbt-adapters/issues/1690))
- Struct aware column list creation in persist_docs now retains case sensitivity for case sensitive validation ([#NA](https://github.com/dbt-labs/dbt-adapters/issues/NA))
