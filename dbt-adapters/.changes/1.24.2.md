## dbt-adapters 1.24.2 - May 21, 2026

### Fixes

- Make validate_doc_columns honor the per-column quote config so quoted identifiers compare case-sensitively instead of being matched after a blanket lowercase ([#1937](https://github.com/dbt-labs/dbt-adapters/issues/1937))
