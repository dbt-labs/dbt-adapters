## dbt-adapters 1.24.1 - May 19, 2026

### Features

- Add generate_latest_version_pointer_alias dispatched macro so users can customize the latest-version pointer name for versioned models ([#1917](https://github.com/dbt-labs/dbt-adapters/issues/1917))
- Add Capability.CatalogsV2 and bridge_v2_catalog template methods on BaseAdapter for adapter-owned v2 catalog bridge architecture ([#1920](https://github.com/dbt-labs/dbt-adapters/issues/1920))

### Fixes

- Fix KeyError on 'javascript' when running dbt on dbt-core < 1.12 after installing dbt-adapters 1.24.0 ([#1926](https://github.com/dbt-labs/dbt-adapters/issues/1926))

### Under the Hood

- Extract the unique-key match used inside `default__get_merge_sql` into a dispatchable `get_merge_unique_key_match` macro so adapters can override the comparison without forking the full merge SQL ([#1902](https://github.com/dbt-labs/dbt-adapters/issues/1902))
